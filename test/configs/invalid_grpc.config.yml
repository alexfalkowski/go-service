environment: production
transport:
  http:
    mux: gateway
    port: 11000
    timeout: 10s
    retry:
      backoff: 100ms
      timeout: 1s
      attempts: 3
    user_agent: Service http/1.0
  grpc:
    port: "invalid"
    timeout: 10s
