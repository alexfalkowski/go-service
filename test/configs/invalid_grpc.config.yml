environment: production
transport:
  http:
    mux: gateway
    port: 11000
    timeout: 10s
    retry:
      backoff: 100ms
      timeout: 1s
      attempts: 3
  grpc:
    port: "invalid"
    timeout: 10s
