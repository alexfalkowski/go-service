// Package strings provides string helpers and small wrappers around the standard library strings package.
//
// This package re-exports a subset of the standard library strings API and adds small convenience helpers.
// See `Bytes` for converting strings to byte slices without allocation (unsafe).
package strings
